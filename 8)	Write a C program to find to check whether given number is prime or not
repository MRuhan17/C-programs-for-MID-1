#include <stdio.h>
int main() {
    int n, i,a=1;
    scanf("%d",&n);
    if(n<=1)
    {
        a=0;
    }
    else
    {
        for(i=2;i*i<=n;i++)
        {
            if(n%i==0)
            {
                a=0;
                break;
            }
        }
        if(a)
        {
            printf("Prime");
        }
            else
        {
            printf("Not Prime");
        }
    }
}
